<h1>Update details</h1>
<%= form_for @user do |f| %>
  <%= f.label :name, 'Username' %>
  <%= f.text_field :name%>
  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.submit "Save" %>

  <% end %>
